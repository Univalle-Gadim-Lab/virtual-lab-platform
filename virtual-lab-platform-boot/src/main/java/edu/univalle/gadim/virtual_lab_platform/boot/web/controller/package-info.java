/**
 * REST controllers for cross-cutting boot module endpoints.
 *
 * <p>Exposes application-level endpoints such as health checks under the
 * {@code /api} base path. These controllers handle infrastructure concerns
 * that are not tied to a specific business domain.
 */
package edu.univalle.gadim.virtual_lab_platform.boot.web.controller;
